.model small
.stack 100h
.data
a db 'My name is Afifa. My ID is:IT22005',0ah,0dh,'$'
.code
main proc
    
    mov ax,@data
    mov ds,ax 
    
    lea dx,a
    mov ah,09h
    int 21h
    
    mov ax,-5
    cmp ax,0
    jl negetive
    
    mov ah,2
    mov dx,ax
    add dx,48
    int 21h
    
    negetive:
    neg ax
    mov ah,2
    mov dx,ax
    add dx,48
    int 21h
    
    exit:
    mov ah,4ch
    int 21h
    main endp
end main
