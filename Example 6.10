.model small
.stack 100h
.data
a db 'My name is Afifa. My ID is:IT22005',0ah,0dh,'$'
.code
main proc
    mov ax,@data
    mov ds,ax 
    
    lea dx,a
    mov ah,09h
    int 21h
    
  read:
    mov ah,1
    int 21h
    
    cmp al,' '
    je exit
    jmp read
    
  exit:
    mov ah,4ch
    int 21h
    main endp
end main
