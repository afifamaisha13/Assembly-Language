.model small
.stack 100h
.data
a db 'My name is Afifa. My ID is:IT22005',0ah,0dh,'$'
.code
main proc
    mov ax,@data
    mov ds,ax
    
    lea dx,a
    mov ah,09h
    int 21h
    
    mov ax,5
    mov bx,9
    
    cmp ax,bx
    jg result
    
    mov cx,bx
    add cx,48
    mov ah,2 
    mov dx,cx
    int 21h
    jmp exit
    
   result:
    mov cx,ax
    add cx,48
    mov ah,2
    mov dx,cx
    int 21h
  exit:
    mov ah,4ch
    int 21h
    main endp
end main
  
                                                                  .
